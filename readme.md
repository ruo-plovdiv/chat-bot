### ChatBot ###

# 🚀 Ръководство за инсталиране на Ubuntu или Linux Mint с Rufus и инсталиране на Ollama + Open-WebUI в Linux

### 1. Изтегляне на Rufus (за Windows)

Изтегли програмата от: https://rufus.ie/

### 2. Изтегляне на Linux дистрибуция
Опция А: Ubuntu

Официален сайт: https://ubuntu.com/download/desktop

Опция Б: Linux Mint

Официален сайт: https://linuxmint.com/download.php


### 3. Подготовка на USB флашка с Rufus

Постави USB устройството в компютъра.

Стартирай Rufus.

Избери:

Device: USB флашката

Boot selection: избери ISO файла, който изтегли

Остави автоматичните настройки:

Partition Scheme: GPT (ако компютърът е UEFI – най-често)

File system: автоматично (обикновено FAT32 или NTFS)

Натисни Start.

Потвърди изтриването на данните на USB устройството.

### 4. Стартиране от USB и инсталация

Рестартирай компютъра.

Влез в BIOS/UEFI менюто с един от клавишите:

F2, F10, F12, Esc или Del

Задай USB устройството като първо в Boot Order.

Запази промените.

Зарежда инсталатора на Ubuntu/Mint.

При стартиране ще видиш:

Install Ubuntu / Linux Mint

Try without installing (Live mode)

Инсталация:

Следвай стъпките на екрана.
Ако не знаеш как да разделяш дискове, избери:

Erase disk and install (Ubuntu/Mint)
⚠ Изтрива всички данни на диска!

🤖 Инсталация на Ollama
## 1. Обновяване на системата

```bash
sudo apt update
sudo apt upgrade -y
```

## 2. Инсталиране на Ollama

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

## 3. Стартиране на модел (пример с Gemma 3)
```bash
ollama run gemma3
```

## 🌐 Инсталация на уеб интерфейс Open-WebUI

Open-WebUI е удобен уеб панел за работа с Ollama.

## 1. Проверка за Python и pip

В повечето Linux дистрибуции Python идва предварително.

Ако pip липсва:

```bash
sudo apt install python3-pip -y
```

Препоръчително:

```bash
pip install --upgrade pip
```

## 2. Инсталация на Open-WebUI

```bash
pip install open-webui
```

## 3. Стартиране на уеб интерфейса

```bash 
open-webui serve
```

След това отвори:

```bash
http://localhost:8080
```